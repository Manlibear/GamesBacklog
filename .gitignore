.venv/
__pycache__/
*.pyc

# personal data / secrets — see config.example.json and README
# library.json, config.json, and downloaded covers now live in
# ~/.local/share/gamesbacklog, not in this repo
